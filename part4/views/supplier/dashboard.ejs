<!DOCTYPE html>
<html lang="he" dir="rtl">
  <head>
    <meta charset="UTF-8" />
    <title>דשבורד ספק</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <div class="container">
      <h1>ברוכים הבאים <strong><%= companyName %></strong>!</h1>

      <p>בחר פעולה</p>

      <div class="actions">
        <form action="/supplier/orders" method="GET">
          <input type="hidden" name="supplierId" value="<%= supplierId %>" />
          <input type="hidden" name="companyName" value="<%= companyName %>" />
          <button type="submit" class="navigate">הצג את ההזמנות שלי</button>
        </form>
      </div>

      <div class="actions">
        <form action="/" method="GET">
          <button type="submit" class="navigate">התנתק</button>
        </form>
      </div>
    </div>
  </body>
</html>
